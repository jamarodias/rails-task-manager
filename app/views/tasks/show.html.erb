<h1>Task info</h1>
    <p><%= @task.title%></p>
    <p><%= @task.completed%></p>
    <p><%= @task.details%></p>

<p>
  <%= link_to 'All Tasks', tasks_path %>
</p>

<p>
  <%= link_to 'Edit a task', edit_task_path(@task) %>
</p>

<p>
  <%= link_to 'Remove a task', task_path(@task), method: :delete, data: { confirm: 'Are you Sure?' } %>
</p>